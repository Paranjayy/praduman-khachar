export const CONFIG = {
  // Prevent transcript text from being shown to avoid unauthorized copying
  HIDE_TRANSCRIPTS: true,
  // Hide the written articles (Writings) section
  HIDE_WRITINGS: true,
};
